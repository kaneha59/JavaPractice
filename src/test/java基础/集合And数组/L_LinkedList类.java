package java基础.集合And数组;

import org.junit.Test;

import java.util.LinkedList;

/**
 * TODO
 * 需要了解链表和双向链表.
 *      相比于ArrayList在内存中连续存储,链表结构在内存中并不是连续存储的,
 *      每一个元素都存有下一个元素的内存地址,这样构成链表,
 *      因此插入新元素时无需移动既有的元素,实现了比ArrayList快得多的插入(删除)操作.
 *      但是相应的,查询操作就比ArrayList慢得多
 * 特点:
 *      增删快,查询慢
 */
public class L_LinkedList类 {
    LinkedList<String> linkedList;
    @Test
    public void test() {

    }
}
